[package]
name = "afs-tests"
version = "0.1.0"
edition = "2021"
description = "Structured compiler bench runner for ARMFORTAS"

[[bin]]
name = "afs-tests"
path = "src/main.rs"

[dependencies]
armfortas = { path = "../.." }
